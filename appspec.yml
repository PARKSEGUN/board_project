version: 0.0  #CodeDeploy  버전 0.0외에는 오류
os: linux
files:
  - source:  / #CodeDeploy에서 전달해 준 파일 중 destination으로 이동시킬 대상 / 은 전체를 의미
    destination: /home/ec2-user/app/step2/zip/ #source에서 파일을 받을 위치, jar를 실행하는 것은 destination에서 옮긴 파일들로 진행
    overwrite: yes  #기존에 파일이 있으면 덮어쓸지 결정